name: cron-job
on:
  push:
    branches:
      - main  # Chạy khi đẩy mã lên nhánh main
  schedule:
    - cron: '*/5 * * * *'  # Chạy mỗi 55 phút
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: every-five-minute
        run: curl -X POST https://bi-cam.vercel.app/api/trigger